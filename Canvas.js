	var rocketx;
	var rockety;
	var rocketPos;


var Angle = 90
var rotationamount = 2
	var speedx;
	var speedy;

	// Size of the rocket
	let rocketSize = 75;

function setup() {
	createCanvas(windowWidth, windowHeight);
	background(100);
	AngleMode(DEGREES)
	frameRate(60)

}