	var angle = 0
	var rotationamount = 2

	// Size of the rocket
	let rocketsize = 75;

function setup() {
	createCanvas(windowWidth, windowHeight);
	background(100);
	angleMode(DEGREES)
	frameRate(60)

}